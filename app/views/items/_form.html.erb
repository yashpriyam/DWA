<%= simple_form_for @item do |f| %>
  <%= f.input :title, label: "Item Title" %>
  <%= f.input :description %>
  <%= f.input :maximum_XPs, label: "Maximum XPs to be allocated" %>
  <%= f.button :submit %>
<% end %>
